-- As usual for configs, playing multiplayer will require all players to have the same config as the server.

-- enable objects to be rotated by other players, default false
dtms.config.allow_rotate = false

-- enable objects to be mined by other players, default false
dtms.config.allow_mining = false

-- enable objects to be shot by other players, default false
dtms.config.allow_shooting = false

-- enable verbose logging, useful only for debugging, default false
-- dtms.config.debug_logging = true
